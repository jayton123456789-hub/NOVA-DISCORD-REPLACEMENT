import { useState } from 'react';
import { Hash, Volume2, X } from 'lucide-react';
type Props={open:boolean; onClose:()=>void; onCreate:(name:string,kind:'text'|'voice')=>void;};
export default function CreateChannelModal(p:Props){const[name,setName]=useState('');const[kind,setKind]=useState<'text'|'voice'>('text');if(!p.open)return null;return <div className="modal-layer"><form className="modal" onSubmit={(e)=>{e.preventDefault();const n=name.trim().toLowerCase().replace(/\s+/g,'-').replace(/[^a-z0-9-_]/g,'');if(n){p.onCreate(n,kind);setName('');p.onClose()}}}><header><div><span className="eyebrow">SPACE SETTINGS</span><h2>Create channel</h2></div><button type="button" onClick={p.onClose}><X/></button></header><div className="channel-kind-picker"><button type="button" className={kind==='text'?'active':''} onClick={()=>setKind('text')}><Hash/><span><b>Text</b><small>Messages and files</small></span></button><button type="button" className={kind==='voice'?'active':''} onClick={()=>setKind('voice')}><Volume2/><span><b>Voice</b><small>Talk, camera, share</small></span></button></div><label>CHANNEL NAME<input autoFocus value={name} onChange={(e)=>setName(e.target.value)} placeholder="dev-room"/></label><button className="modal-primary" type="submit" disabled={!name.trim()}>CREATE CHANNEL</button></form></div>;}
