import { Headphones, Mic, MicOff, MonitorUp, PhoneOff, Video } from 'lucide-react';
type Props={connected:boolean; channelName?:string; muted:boolean; cameraOn:boolean; sharing:boolean; onMute:()=>void; onCamera:()=>void; onShare:()=>void; onLeave:()=>void;};
export default function VoiceDock(p:Props){ if(!p.connected)return <div className="voice-dock idle"><span><i/>VOICE READY</span></div>; return <div className="voice-dock"><div className="voice-connected"><span className="signal"><i/><i/><i/></span><div><b>VOICE CONNECTED</b><small>{p.channelName}</small></div><button className="leave-mini" onClick={p.onLeave}><PhoneOff size={14}/></button></div><div className="voice-buttons"><button className={p.muted?'danger active':''} onClick={p.onMute}>{p.muted?<MicOff size={15}/>:<Mic size={15}/>}</button><button className={p.cameraOn?'active':''} onClick={p.onCamera}><Video size={15}/></button><button className={p.sharing?'active':''} onClick={p.onShare}><MonitorUp size={15}/></button></div></div>; }
